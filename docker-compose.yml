version: '3.8'

services:
  api:
    build: .
    ports:
      - "8688:8688"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
    # 如果需要挂载本地文件进行测试
    # volumes:
    #   - ./data:/app/data

